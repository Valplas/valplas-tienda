{
  "mcpServers": {
    "supabase-db": {
      "command": "npx",
      "args": ["-y", "@supabase/mcp-server-supabase", "${DATABASE_URL}"],
      "env": {
        "SUPABASE_URL": "${SUPABASE_URL}",
        "SUPABASE_SERVICE_KEY": "${SUPABASE_SERVICE_KEY}"
      }
    },
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "C:\\Programacion\\ValplasTienda\\valplas-tienda"
      ]
    }
  }
}
